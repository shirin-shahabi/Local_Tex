% Minimal style file for TruthTensor whitepaper
% This file provides basic formatting for the paper

\ProvidesPackage{truthtensor_whitepaper}[2025/12/10 TruthTensor Whitepaper Style]

% Basic packages that might be needed
\RequirePackage{hyperref}
\RequirePackage{url}
\RequirePackage{graphicx}
\RequirePackage{booktabs}
\RequirePackage{multirow}
\RequirePackage{balance}

% Hyperref setup
\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue,
    pdftitle={TruthTensor: Evaluating LLM Human Imitation Through Prediction Market Drift and Holistic Reasoning},
    pdfauthor={Shirin Shahabi, Spencer Graham, Haruna Isah}
}

% URL formatting
\urlstyle{same}







